# 변경 이력

이 프로젝트의 버전별 변경 사항을 기록합니다. [Keep a Changelog](https://keepachangelog.com/) 형식을 따르되, 개인 프로젝트 특성상 날짜/커밋 기준으로 단순화했습니다.

## [1.6.7] - 2026-08-28

### 변경 (테스트 시나리오 생성기)
- 다운로드 파일명 끝에 붙던 generationId 앞 8자리(예: `_a3f9c2`)를 제거 — 사용자 요청. 날짜는 그대로 유지해 최소한의 구분은 남지만, 같은 날 같은 서비스/화면명으로 재생성하면 파일명이 다시 겹칠 수 있음(의도된 트레이드오프).
- 범례 시트를 열었을 때 포커스 셀이 템플릿에 저장된 값(F8)이 아니라 항상 A1이 되도록 수정.

## [1.6.6] - 2026-08-28

### 수정 (배너 영역을 알럿으로 잘못 분류)
1.6.5에서 잡은 "반말체 오판"의 진짜 근본 원인 — 사용자 확인: "최대 300만 포인트 받기!"는 알럿이 아니라 롤링 배너 영역(캐러셀 '1/5' 표시, Description에도 "배너 영역"이라고 명시)에 적힌 예시 문구였는데, 검수가 이를 "알럿" 컴포넌트로 잘못 분류해서 검토·톤 판단 근거로 사용했다.

- 배너/프로모션 슬롯(캐러셀 페이지 표시, Description에 "배너 영역"·"롤링배너" 언급)을 '알럿'과 구분해 component_type을 '배너'/'프로모션'으로 정확히 분류하는 규칙 추가.
- 배너 슬롯 문구는 여러 개가 순환 노출되는 예시 카피 중 하나일 뿐 최종 확정 카피가 아닐 수 있으므로, 오탈자 등 명백한 경우가 아니면 어투 지적을 severity 낮게 하거나 보고하지 않도록 함.
- 톤앤매너 감지 시에도 배너/프로모션 슬롯 문구는 근거에서 제외하도록 명시.
- 재검증 완료: 같은 문서에서 해당 문구가 이제 'component_type: 배너'로 정확히 분류되고, severity도 low로 신중하게 처리됨을 확인.

## [1.6.5] - 2026-08-28

### 수정 (반대 방향의 톤 오판 — 존댓말 문서를 반말체로 오인식)
직전 수정(1.6.4)의 반대 방향 문제: "땡겨요_검색" 문서를 "간결하고 직관적인 반말체"로 잘못 감지해, 실제로는 정상적인 존댓말 알럿("집주소를 변경하시겠어요?")을 진짜 반말("집주소를 변경할까?")로 바꾸라고 제안하는 이슈가 발생.

- **원인**: 실제 화면 문구를 직접 확인한 결과 존댓말만 일관되게 쓰이고 있었다("~해 보세요", "~있습니다", "~변경하시겠어요?" 등, 반말 종결어미는 어디에도 없음). 톤 감지가 배너/타이틀 문구("최대 300만 포인트 받기!", "나라도 나도 땡기는 싸이버거 100원" 같은 명사형·말장난 카피)의 발랄한 인상을 문법적 "반말"과 혼동한 것으로 확인 — 배너 카피는 완전한 문장이 아니라 종결어미 자체가 없어서 존댓말/반말 판단 근거가 될 수 없는데도 근거로 사용됨.
- **수정**: 존댓말/반말은 "발랄함" 같은 인상이 아니라 문장 종결어미(~습니다/~해요 = 존댓말, ~야/~어/~다 = 반말)로만 판단하도록 명시하고, 배너·타이틀처럼 종결어미가 없는 문구는 판단 근거에서 제외하도록 톤 감지·문구 검수 프롬프트에 규칙 추가. 완전한 문장을 찾을 수 없으면 존댓말/반말 판단 자체를 생략하도록 함.
- **검증 완료**: 같은 문서로 재검증 — 톤이 "존댓말 체"로 정확히 감지되었고, 문제였던 두 알럿 모두 존댓말을 유지한 채 문장만 간결화하는 정상적인 제안으로 바뀜을 확인.

## [1.6.4] - 2026-08-28

### 수정 (톤 오판·불필요한 정책페이지 검토·근거 없는 지적·일관성 환각)
사용자가 4가지 문제를 함께 제기: ① 톤앤매너 기준선 자체가 틀림 → 그 여파로 정상 문구를 전부 지적, ② 제안 문구가 원문과 사실상 동일("고치는 척"), ③ 정책/체크리스트 페이지를 실제 화면처럼 검토, ④ 일관성 요약에 문서에 없는 내용(다른 화면 두 개를 하나로 합쳐 존재하지 않는 문구를 생성)이 등장.

- **원인 확인(실제 데이터로 재현)**: 문제의 "땡겨요_주문하기" 문서는 1~20페이지가 SCREEN/SCREEN ID 헤더 없는 정책·체크리스트 표인데, 그중 "결제하기 버튼 탭 시 체크 항목" 표에 실제 Alert 문구 예시가 인용돼 있어 톤 자동 감지가 이 표를 "진짜 화면 문구"로 오인 — 실제 화면(21p~)은 "~해요"체가 지배적인데 기준선이 "격식체"로 고정되어 이후 정상 문구가 전부 위반으로 지적됨. ④는 배치 로그를 직접 남겨 확인한 결과 문서 간 오염이 아니라, 일관성 통합 단계(162개 인스턴스를 한 번에 비교)에서 서로 무관한 두 화면(9화면 "주문 취소", 10화면 "결제완료")을 하나의 "로그인 버튼" 패턴으로 잘못 묶고 "취소"+"완료"를 조합해 존재하지 않는 "취소완료"를 만들어낸 환각으로 확인.
- **③ 정책 페이지 제외**: "SCREEN/SCREEN ID/UI TYPE 헤더가 없으면 실제 문구가 인용되어 있어도 예외 없이 제외" 규칙을 문구 검수·톤 감지 프롬프트에 추가. 재검증 결과 오탐지됐던 화면 라벨 다수가 사라짐.
- **① 톤 오판**: 프롬프트 지시만으로는(정책 페이지면 빈 문자열 출력) 신뢰할 수 없음을 3차례 재현으로 확인 — 실제 Alert 문구가 인용된 페이지를 만나면 지시를 무시하고 판단을 강행. 구조화 출력 스키마 자체에 `has_real_screen`(실제 화면 존재 여부) 필드를 강제로 추가하고, 코드에서 이 값이 false면 모델이 뭐라고 답했든 무시하도록 변경 — 프롬프트 준수가 아니라 코드 레벨에서 강제.
- **② 제안 문구 품질**: "이슈가 '문장이 길고 복잡함'이라면 제안도 실제로 짧고 단순해야 한다 — 조사/어미만 바꾸는 수준은 해결이 아니다" 규칙 추가.
- **④ 일관성 환각**: "버튼처럼 넓은 카테고리 안에서도 목적이 다른 항목(로그인/주문 취소/결제완료)은 절대 하나로 묶지 않는다", "pattern·note의 모든 문구는 인스턴스 목록의 text 값을 그대로 인용해야 하며 서로 다른 인스턴스를 조합해 새 문구를 만들지 않는다" 규칙 추가.
- **부수 발견/수정**: `/api/review/[runId]`가 `service_name_detected` 컬럼을 응답에 포함하지 않던 누락 수정. 톤/서비스명 자동 감지 호출에 OpenAI 요청 한도(크레딧 부족 포함) 에러 처리 추가.
- **검증 완료**: 크레딧 충전 후 같은 문서로 재검증 — 톤이 "간결하고 직관적인 반말체, 이모지 사용"으로 정확히 감지되었고, 그 결과 이전에 오지적됐던 "~해요"체 정상 문구 대부분이 더 이상 지적되지 않음. 제안 문구도 실제로 더 짧아졌고, 일관성 요약에서도 로그인/취소완료 환각이 재현되지 않음. (아주 드물게 톤과 맞는 정상 문구 1건이 배치 단독 판단에서 여전히 지적되는 잔여 사례는 남아있음 — 배치별 판단이 문서 전체 맥락을 다 보지 못하는 구조적 한계로, 완전히 근절하기는 어려움.)

## [1.6.3] - 2026-08-28

### 수정 (문구 인용 오독)
- 실제 검수 결과에서 "매장 식사 준비중"을 "매장사사 준비중"으로, "지역사랑"을 화면마다 "지역사항"/"지역사용"으로 서로 다르게 잘못 읽어 존재하지 않는 오탈자·불일치를 보고하던 문제.
- 원인 확인을 위해 실제 문서를 원본 해상도(1920×1080)로 렌더링해 픽셀 단위로 직접 확인 — 이미지 자체는 완전히 선명해서 렌더링 해상도 문제가 아니라 비전 모델의 순수한 글자 인식 오류로 판명.
- 형태가 비슷한 한글(사/식, 랑/항/용 등)을 혼동하지 않도록 한 글자씩 재확인하고, 읽은 결과가 사전에 없거나 뜻이 안 통하면 오독으로 의심해 재확인하거나 보고하지 않도록 프롬프트에 추가.
- 일관성 판단 단계에도 안전망 추가: 한두 글자만 다르고 한쪽이 뜻이 안 통하는 조합이면 실제로는 같은 문구의 오독일 가능성이 높다고 보고 불일치로 보고하지 않도록 함.
- 실제 사용자가 겪은 원본 문서로 재검증 — 두 오독 모두 재현되지 않음을 확인.

## [1.6.2] - 2026-08-28

### 수정 (실사용 검수 결과 재검증)
- **서비스 자체 브랜드명 오판**: 실제 "땡겨요" 화면설계서에서 자사 로그인 버튼("땡겨요 로그인")을 카카오/네이버 등 타사 로그인과 나란히 쓴 정상 표기를 "비공식적/모호한 표현"으로 잘못 지적하던 문제. 배치 0에서 서비스명을 한 번 자동 감지해 모든 배치 프롬프트에 주입(`service_name_detected` 컬럼 추가)하고, 프롬프트에도 "반복 등장하는 자체 서비스명은 정상 브랜딩이므로 지적하지 않는다"는 규칙을 명시.
- **근거 없는 어투 지적**: "입력하신 정보를 다시 확인해 주세요."처럼 이미 통용되는 정중한 존댓말을 "비격식적"이라며 지적하던 문제 → 어투 관련 이슈는 같은 문서 안에 실제 대조 사례가 있을 때만 보고하도록 프롬프트에 명시.
- **부수 발견**: 실제 대용량 문서 재검증 중 배치 응답이 gpt-4o의 출력 토큰 한도에 걸려 JSON이 잘리는 경우를 발견 — 문구 검수 배치 크기를 12페이지 → 6페이지로 낮추고(`REVIEW_BATCH_SIZE`), 길이 제한으로 잘렸을 때 원인을 구분해서 안내하는 에러 메시지 추가.
- 실제 사용자가 겪은 원본 문서로 재검증(13개 배치 모두 정상 완료) — 두 문제 모두 재현되지 않음을 확인. 다만 배치별 판단이 문서 전체의 일관성 정보를 아직 모르는 시점에 이뤄지는 구조적 한계로, 다른 배치에서는 이미 정상으로 확인된 표현을 특정 배치 하나가 드물게 지적하는 사례는 완전히 배제되지 않음.

## [1.6.1] - 2026-08-28

### 수정 (문구 검수 품질)
- **일관성 판단 자기모순 문제**: 배치마다 독립적으로 일관성을 판단하고 기계적으로 병합하던 방식 → 배치는 findings와 컴포넌트 인스턴스 전체(`component_instances`)만 수집하고, 모든 배치가 끝난 뒤 텍스트 전용 단일 호출(`synthesizeConsistency`)로 전체를 한 번에 비교해 일관성을 판단하도록 재설계. 판단 지점이 하나뿐이라 배치 간 모순이 구조적으로 발생하지 않음.
- **컴포넌트 문구 인용 정확도**: 프롬프트에 "컴포넌트 시각적 경계 안쪽 글자만 인용, 화면 제목/주제로 문구를 짐작하지 말 것"이라는 인용 가드레일과 구체적 반례 예시를 추가.
- **screen 필드 포맷 고정**: '[페이지번호] 화면명' 형식만 허용, 다른 표기 금지를 프롬프트에 명시.
- **temperature 0으로 고정**: 검수는 창의성보다 일관성·정확성이 중요해 모든 검수 관련 호출(문구 검수, 톤앤매너 감지, 일관성 종합)에 적용.
- `OPENAI_MODEL` 확인 결과 `gpt-4o` 사용 중 (환경변수 미설정, 코드 기본값).
- 실제 검수 파이프라인으로 재현 테스트 중 한글이 깨져 렌더링된 테스트 이미지 때문에 잘못된 환각처럼 보였던 사례를 발견 — 폰트를 교체해 재검증한 결과 실제로는 컴포넌트 문구를 정확히 인용하고, 화면 간 일관성 불일치도 근거(어느 화면이 어떻게 다른지)와 함께 하나의 일관된 판단으로 보고됨을 확인.

## [1.6.0] - 2026-08-28

### 추가
- **검수하기** 기능 신설 (`DEVELOPMENT_PLAN.md` Phase 0~2, Phase 6 일부) — 상단 네비게이션에 "검수하기" 메뉴 추가.
  - **문구 검수** (`/review/copy`): PDF·이미지 업로드 → 버튼/알럿/placeholder 등 컴포넌트 문구의 적절성을 톤앤매너 기준으로 판단하고 대체 문구를 제안. 톤앤매너를 직접 입력하지 않으면 문서(첫 배치)를 분석해 자동 감지.
  - **디자인 연동 검수** (`/review/figma`): Figma 연동은 아직 미착수 — 준비 중 안내만 표시.
  - `/review/[runId]`에서 검수 결과 리포트(문구 이슈 표, 화면 간 일관성 요약) 확인, `/history`에 검수 이력 탭 추가.
  - `review_runs` 테이블 신설, 시나리오 생성기와 동일한 배치(12페이지) 단위 재개형 처리(`progress_current`/`review_partial`)를 처음부터 적용해 대용량 문서에서도 메모리/타임아웃 위험 없음.
  - 업로드 경로는 기존 `test-scenario-files` 버킷을 `review/<id>/` 프리픽스로 재사용 (새 버킷/정책 불필요).

## [1.5.1] - 2026-08-28

### 수정
- 1.5.0 배포 후에도 같은 79페이지 문서에서 "생성 실패 (HTTP 500)"가 재발 — 배치 단위 처리로도 특정 배치(3번째) 하나가 여전히 메모리 부족으로 죽는 문제.
  - 로컬에서 재현해보니 원인은 배치 개수가 아니라 그 배치 안 특정 페이지(고해상도 이미지가 포함된 슬라이드)의 래스터화 자체가 순간적으로 RSS를 약 1.7GB까지 튀게 만드는 것 — Vercel Hobby의 2GB 한도에 거의 닿아 있었음.
  - PDF 페이지 래스터화 배율을 1.5 → 1.0으로 낮춰 같은 페이지의 순간 최대 메모리를 약 1GB로 절반 가까이 줄임 (텍스트 가독성은 유지).
  - 배치 2/7까지 처리된 채 멈춰 있던 실제 생성 건을 같은 generationId로 재호출해 나머지 배치(이전에 죽던 3번째 배치 포함)까지 프로덕션에서 정상 완료되는 것을 확인 (시나리오 56개, 스텝 154개).

## [1.5.0] - 2026-08-28

### 변경
- 화면설계서 페이지 수(또는 이미지 장수) 제한을 없애고, 문서 크기와 무관하게 처리 가능하도록 생성 방식을 재설계.
  - `/api/generate`가 문서 전체를 한 번의 요청에서 처리하던 방식 → 배치(12페이지) 1개씩만 처리하고 반환하는 방식으로 변경. 진행 상황(`extraction_partial`, `progress_current`)을 DB에 누적 저장해두고, 업로드 화면이 다음 배치를 자동으로 이어서 요청.
  - Vercel 함수 제한 시간(300초)이 더 이상 "문서 전체를 몇 페이지까지 처리 가능한가"의 상한이 되지 않음 — 배치가 많아지면 요청 횟수만 늘어날 뿐, 개별 요청 시간은 항상 배치 1개 분량으로 고정.
  - 기존에 진행 상황을 별도로 폴링하던 `/api/status/[id]` 엔드포인트는 더 이상 필요 없어 제거 (배치 응답에서 바로 진행률을 받음).
  - 실제로 이전에 메모리 부족(1.4.1)으로 실패했던 79페이지 문서로 재검증 — 7개 배치로 나뉘어 정상 완료(시나리오 47개, 스텝 130개).

## [1.4.1] - 2026-08-28

### 수정
- 79페이지(30MB)짜리 화면설계서 PDF에서 "생성 실패 (HTTP 500)" — 함수가 메모리 부족으로 강제 종료되던 문제.
  - 페이지 수 제한(60페이지) 검사를 페이지를 전부 래스터화한 뒤가 아니라, `pdf-to-img`가 렌더링 없이 즉시 알려주는 `document.length`로 맨 처음에 하도록 변경 — 초과 시 렌더링을 시작하기도 전에 명확한 오류로 실패.
  - 전체 문서를 한 번에 래스터화해 메모리에 쌓아두던 방식 → 배치(12페이지)마다 필요한 페이지만 그때그때 렌더링해서 OpenAI에 보내고 즉시 해제하는 방식으로 변경, 대용량 문서의 최대 메모리 사용량을 크게 낮춤.
  - Vercel Hobby 플랜은 Active CPU 과금 방식이라 `vercel.json`의 `functions.memory` 설정이 무시된다는 걸 확인 — 해당 설정은 제거하고 실제 메모리 사용량을 줄이는 방향으로 해결.

## [1.4.0] - 2026-08-28

### 추가
- 화면설계서 업로드 시 PDF 외에 이미지(JPG/PNG/WEBP, 최대 30장)도 직접 업로드해 추출할 수 있도록 지원. `lib/ai/extract.ts`의 PDF 래스터화 이후 로직을 공통 함수로 분리하고, `app/api/upload-url`이 여러 파일을 하나의 `generationId`로 묶어 업로드받을 수 있도록 확장.
- 업로드 폼에 등록 가능한 파일 형식/개수 안내 문구 추가(PDF 1개 또는 이미지 최대 30장, PPT는 PDF 변환 후 업로드).

### 수정
- 생성된 엑셀이 항상 마지막에 만진 시트(범례)가 활성 탭으로 열리던 문제 → 빌드 시 표지를 활성 시트로 강제 고정.
- 이력 페이지에서 다운로드할 때 한글 파일명이 깨지던 문제 → Supabase 서명 URL의 `download` 옵션(RFC 5987 미인코딩)에 의존하지 않고 파일을 직접 스트리밍하며 `Content-Disposition`을 명시적으로 인코딩.
- 동일 서비스/화면 범위로 재생성 시 파일명이 겹쳐 다운로드 폴더에서 이전 파일을 덮어쓰던 문제 → 파일명에 생성일자 + generationId 앞 8자리를 추가해 매 생성마다 고유하게 만듦.

## [1.3.2] - 2026-08-27

### 수정
- 생성 일시를 2줄(날짜/시간)로 표시. 원본 파일 열 너비 축소, 상태 열은 남는 공간을 흡수하도록 유동 폭으로 변경.
- 페이지 좌우 여백을 절반으로 줄이고 콘텐츠 최대 너비를 880px → 1120px로 넓혀 이력 표에 더 많은 공간 확보 (업로드 폼은 별도로 560px 폭 유지).

## [1.3.1] - 2026-08-27

### 수정
- 생성 이력 표에서 "단계 / 스텝", "다운로드" 열이 좁아 줄바꿈되던 문제. `table-layout: fixed` + 열별 고정 너비(`<colgroup>`)로 조정하고, 원본 파일명 열은 넘치면 말줄임(ellipsis)되도록 변경.

## [1.3.0] - 2026-08-27

### 추가
- `DESIGN_SYSTEM.md` — 토스증권(corp.tossinvest.com) 스타일(미니멀 산세리프·굵은 헤드라인·절제된 포인트 컬러)에 블랙 배경 + 글래스모피즘을 결합한 다크 테마 디자인 시스템 문서.
- `app/globals.css` — 컬러/타이포/간격 토큰과 `.glass`, `.btn-primary`, `.badge-*`, `.table` 등 재사용 클래스.

### 변경
- 업로드 화면(`app/page.tsx`), 이력 화면(`app/history/page.tsx`), 전역 레이아웃(`app/layout.tsx`)을 새 디자인 시스템으로 전면 리디자인. 폰트는 Noto Sans KR(`next/font/google`)로 교체.

## [1.2.0] - 2026-08-27

### 변경
- PDF 추출을 페이지 12개 단위 배치로 나눠 순차 처리하도록 변경. OpenAI 계정의 분당 토큰 한도(TPM)를 한 번의 요청이 넘어서면(예: Tier 1의 30,000 TPM) 재시도해도 항상 실패하는 문제라, 요청 자체를 잘게 쪼개는 방식으로 해결.
- 배치별 결과를 합친 뒤 시나리오 단계 번호(`1.<n> 제목`)를 전체 문서 기준으로 다시 순서대로 부여.
- 업로드 화면에 배치 진행률("배치 N/M 처리 중")을 1.5초 간격 폴링으로 표시.

### 추가
- `app/api/status/[id]` — 생성 진행 상태(현재 배치/전체 배치) 조회용 엔드포인트.
- `generations` 테이블에 `progress_current`, `progress_total` 컬럼 추가.

## [1.1.6] - 2026-08-27

### 변경
- OpenAI 429(rate limit) 에러 시 SDK 재시도 횟수 증가(`maxRetries: 5`), 재시도로도 실패하면 원인을 명확히 설명하는 한글 에러 메시지로 대체.

## [1.1.5] - 2026-08-27

### 수정
- `pdf-to-img` 문서 객체를 `destroy()`하지 않아 pdfjs 내부 렌더링 버퍼가 요청이 끝날 때까지 메모리에 남아있던 문제 → 배치 렌더링 후 명시적으로 해제.
- 페이지 렌더링을 PNG·scale 2 → JPEG·scale 1.5로 낮춰 메모리 사용량 감소. 페이지 수가 많은 화면설계서에서 발생하던 Vercel 함수 OOM(메모리 초과) 해결.

## [1.1.4] - 2026-08-27

### 수정
- `/history`가 항상 빈 목록을 반환하던 문제. Next.js가 Supabase 클라이언트 내부 `fetch` 호출까지 자동 캐싱해서, 데이터가 없던 초기 시점의 빈 응답이 계속 캐시되어 나오고 있었음 → `cache: 'no-store'` 명시로 해결.
- 생성된 xlsx를 Supabase Storage에 올릴 때 한글 파일명을 그대로 저장 키로 써서 업로드가 거부되던 문제(`Invalid key`) → 저장 키는 ASCII 고정, 실제 한글 파일명은 별도 컬럼(`output_filename`)으로 분리해 재다운로드가 항상 실패하던 것을 해결.

## [1.1.3] - 2026-08-27

### 수정
- `pdf-to-img`가 런타임에 `require.resolve('pdfjs-dist/package.json')`로 참조하는 파일이 Vercel 배포 번들 추적(`@vercel/nft`)에서 빠져 있던 문제 → `outputFileTracingIncludes`로 `pdf-to-img`/`pdfjs-dist` 패키지 전체를 강제 포함.

## [1.1.2] - 2026-08-27

### 수정
- `@napi-rs/canvas`(pdfjs-dist가 PDF 페이지를 렌더링할 때 쓰는 네이티브 캔버스 모듈)가 프로덕션 번들에서 빠져 `DOMMatrix is not defined`로 실패하던 문제 → `serverComponentsExternalPackages`에 추가해 webpack이 네이티브 바이너리를 잘못 번들링하지 않도록 수정.

## [1.1.1] - 2026-08-27

### 변경
- PDF 업로드 방식을 서버 함수 경유(FormData) → 브라우저에서 Supabase Storage로 직접 업로드(서명된 URL)로 변경. Vercel 서버리스 함수의 요청 본문 4.5MB 하드 리밋 때문에 실제 크기의 화면설계서 PDF가 거의 항상 413로 실패하던 문제 해결.
- 새 API 라우트 `app/api/upload-url` 추가, `/api/generate`는 파일 대신 스토리지 경로(`generationId`)를 받도록 변경.
- 다운로드 파일명 한글 깨짐 수정 — 클라이언트가 `X-Output-Filename` 헤더를 디코딩하지 않고 그대로 쓰던 버그, macOS가 한글 파일명을 분해형(NFD) 유니코드로 보고하는 문제(→ NFC 정규화)를 함께 해결.
- 엑셀 내 시나리오 텍스트가 템플릿 예시 행의 기울임/회색 스타일을 그대로 물려받던 것을 고쳐, 새로 생성되는 내용은 항상 검은색·기울임 없이 출력.
- 화면ID가 없을 때의 표기 규칙을 템플릿 가이드 문구(`'[화면명]' 형식`)와 일치하도록 AI 추출 규칙/스키마 통일.

## [1.1.0] - 2026-08-27

### 변경
- 시나리오 추출 엔진을 Anthropic Claude(PDF 네이티브 입력) → OpenAI(`gpt-4o`, PDF를 페이지별 이미지로 변환 후 vision 입력 + structured output)로 전환.
- 환경변수 `ANTHROPIC_API_KEY`/`ANTHROPIC_MODEL` → `OPENAI_API_KEY`/`OPENAI_MODEL`.

## [1.0.0] - 2026-08-27

### 추가
- 초기 스캐폴딩: 화면설계서(PDF) 업로드 → AI 시나리오 추출 → 고정 서식 엑셀 생성 → Supabase 저장/이력 조회 Next.js 앱.
